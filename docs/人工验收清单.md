# 人工验收清单

这份清单用于验证真实 Windows 输入是否能从 Hook 进入 SQLite，再实时刷新到 Vue 页面。自动化测试无法模拟真实物理输入，因此接手 agent 或发布前应执行一次。

## 启动

- [x] 执行 release 程序，程序正常打开（npm run tauri dev 留给开发者日常流程）
- [x] 顶部状态显示“本地实时数据”（Hook 成功时）
- [ ] 记录开始前的“总按键数”和“鼠标操作”

## 键盘

- [x] 真实打字过程中各字母键计数正确（用户实际输入验证）
- [x] 长按/自动重复不会放大统计（去重过滤器 + 真实输入未见异常放大）
- [ ] 松开后再次按 `A`，次数再次增加 1
- [x] Shift/Ctrl/Space 等修饰键计数正确（含左右区分）
- [ ] 快速使用组合键时，界面不会卡顿或崩溃

## 鼠标

- [x] 真实鼠标点击/滚轮统计正确（用户实际使用验证：left-click/wheel 计数）
- [ ] 右键点击，右键次数增加
- [ ] 滚轮上下滚动，滚轮统计增加
- [ ] 如果设备有侧键，侧键统计增加

## 控制和持久化

- [x] 界面按钮暂停/继续状态切换正确
- [x] 暂停后统计不增加（托盘与界面按钮路径均验证）
- [ ] 点击“已暂停”恢复记录后，计数继续增加
- [x] 托盘“清空本地统计”与界面清空后数据归零、冠军显示“暂无”
- [x] 重启应用后历史统计保留（多轮重启验证）
- [x] 今天/本周/本月切换后统计范围正确变化
- [x] 自定义日期范围查询结果正确（含跨月数据）
- [x] 自定义范围边界与排除范围外数据验证通过（8 月范围仅含 8 月数据）
- [x] 倒序日期显示错误提示且不改变当前范围

## 隐私与权限说明

- [x] KP 头像按钮弹出隐私与权限说明对话框
- [x] Esc 关闭验证通过
- [x] × 按钮与点击外部区域关闭均验证通过
- [x] 弹窗内容包含三类承诺、权限提示与数据库路径

## 系统托盘

- [x] 通知区域出现 KeyPulse 图标（tooltip “KeyPulse 键鼠热力图”）
- [x] 托盘“打开 KeyPulse”恢复并聚焦窗口
- [x] 关闭按钮隐藏窗口，进程保持运行
- [x] 左键点击托盘图标恢复窗口
- [x] 托盘暂停/继续与主界面状态同步
- [x] 托盘清空后界面实时归零
- [x] 托盘“退出 KeyPulse”进程正常结束

## 限制场景

- [x] 普通权限常规程序统计正常（真实使用验证）
- [x] 限制已在应用内隐私弹窗与 README 中记录
- [x] 监听失败路径代码已验证（状态接口与警告文案），真实高权限场景待人工
- [ ] 监听不可用时顶部显示“监听不可用”，记录按钮不可点击，历史统计仍可查看

## 安装包

- [ ] 使用 MSI 安装包完成安装（需要管理员权限，静默安装 1603，待人工在管理员会话执行）
- [ ] 使用 NSIS 安装包完成安装（需要管理员权限，待人工执行）
- [ ] 卸载流程（待安装后执行）

## 验收记录

```text
日期：2026-09-04（自动验收会话）
系统：Windows 11 x64（3840x2160 @ 300% DPI）
结果：部分通过——真实输入、日期范围、托盘、隐私弹窗、注入过滤已通过；MSI/NSIS 安装卸载因当前会话无管理员权限未执行
问题：
- ...

后续动作：
- ...
```
