# 开发日志

## 2026-09-04：项目启动与方案确认

### 本次完成

- [x] 在 `F:\TauriProject` 下建立并确认项目目录 `KeyboardMouseHeatmap`
- [x] 将原先位于 `F:\KeyboardMouseHeatmap` 的目录移动到 `F:\TauriProject\KeyboardMouseHeatmap`
- [x] 确认使用 Tauri 2 + Rust + Vue 3 + TypeScript + SQLite
- [x] 确认项目需要键盘统计、鼠标统计、键盘/鼠标模板和模板数据展示
- [x] 确认主题需要活泼、丰富、年轻化
- [x] 建立 docs、notes、backups、src、tests 目录
- [x] 写入开发流程、需求清单、技术决策、README、交接说明和备份说明
- [x] 检查开发环境：Node.js/npm 可用，Rust/Cargo 已安装
- [x] 生成 Tauri 2 + Vue 3 + TypeScript 基础模板
- [x] 将基础模板合并到正式项目目录
- [x] 将项目包名、Rust crate 名称和窗口标题改为项目名称
- [x] 添加 Git SSH 远程仓库 `origin`
- [x] 使用 HTML section/table 风格整理 README

### 验证结果

- 项目路径存在：`F:\TauriProject\KeyboardMouseHeatmap`
- 现有 Tauri 参考项目：`F:\TauriProject\SerialTool`
- Node.js：`v26.1.0`
- npm：`11.13.0`
- Rust/Cargo：已通过系统路径探测确认可用，具体版本将在初始化工程时记录
- Tauri 脚手架：已成功生成并合并
- Git 远程：`git@github.com:lsx666xsl/KeyboardMouseHeatmap.git`
- Tauri CLI：`2.11.4`
- Tauri：`2.11.5`
- Rust/cargo：`1.95.0`
- `npm run build`：通过
- `cargo test`：通过，当前暂无测试用例
- `npm run tauri build`：release executable 编译通过；安装包阶段失败

### 遇到的问题

- 初次使用命令行版本探测时，PowerShell 的 `Get-Command` 没有直接显示 Cargo；通过 `where.exe` 确认 Cargo 位于 Rust 工具链目录，后续命令必要时使用完整路径或刷新 PATH。
- 脚手架目录合并时，包含递归删除的单次命令被安全策略拦截；已改为分步复制，暂时保留 `_tauri_scaffold` 作为本地临时目录，并在 `.gitignore` 中排除。
- 初次前端构建发现 `public` 被错误复制成单个 SVG 文件，已恢复为 `public` 目录并重新构建通过。
- 初次 Rust 测试发现 `main.rs` 仍引用脚手架 crate 名称，已改为 `keyboard_mouse_heatmap_lib` 并测试通过。
- Tauri release 构建在 `src-tauri/target/release/keyboard-mouse-heatmap.exe` 成功生成；MSI/NSIS bundling 收尾时出现 `系统无法将文件移到不同的磁盘驱动器。 (os error 17)`，需要后续统一临时目录盘符或单独指定 bundle 目标。

### 下一步

1. 建立 Rust 输入监听模块接口，但先不保存原始事件
2. 实现键盘和鼠标事件的去重、归一化和统计聚合
3. 用 Tauri Event 把真实统计推送到当前可视化首页
4. 建立 SQLite 数据表和历史查询
5. 记录输入监听限制并创建输入监听阶段备份

### 备份/提交

- Git commit：`71a7111 docs: establish project plan and handoff records`
- 规划文档、基础工程和可视化原型提交待本次验证后生成
- release executable 已验证，完整安装包仍未验证
- 规划阶段快照：`backups/2026-09-04-planning.zip`
- 快照基于上述文档初始提交生成

## 2026-09-04：Tauri 原型、README 与远程仓库

### 本次完成

- [x] 生成并合并 Tauri 2 + Vue 3 + TypeScript 工程
- [x] 完成 KeyPulse 可视化首页原型，包含键盘模板、鼠标模板、统计卡片、排行和时间柱状图
- [x] 使用 HTML5 `section`、`table`、`blockquote` 等元素润色 README
- [x] 完成演示数据主题和活泼年轻化配色
- [x] 通过前端构建：`npm run build`
- [x] 通过 Rust 测试：`cargo test`
- [x] 成功生成 release executable：`src-tauri/target/release/keyboard-mouse-heatmap.exe`
- [x] 配置 SSH 远程：`git@github.com:lsx666xsl/KeyboardMouseHeatmap.git`
- [x] 安全合并远程已有的 `Initial commit`
- [x] 成功推送 `main` 到 GitHub，并设置上游跟踪分支

### 提交记录

- `5c3b0af feat: initialize tauri heatmap dashboard prototype`
- `2679c75 merge: preserve remote repository initialization`
- 远程推送结果：`03d0096..2679c75 main -> main`

### 发布构建说明

- release executable 已生成并可作为当前阶段产物
- MSI/NSIS 安装包仍未完成，失败原因是 Windows 构建过程中的跨磁盘移动错误：`os error 17`

### 当前未完成

- [ ] Rust 全局键盘监听
- [ ] Rust 全局鼠标监听
- [ ] SQLite 聚合统计
- [ ] 将真实统计接入当前演示首页

### 下一步

1. 建立 `input` Rust 模块和统一输入事件模型
2. 先实现键盘监听，并增加按键去重单元测试
3. 再实现鼠标点击/滚轮监听
4. 增加 SQLite 数据层和 Tauri 查询/推送接口
5. 创建输入监听阶段备份并再次推送

### 当前备份

- Tauri 基础工程/可视化原型快照：`backups/2026-09-04-tauri-prototype.zip`
- 快照对应提交：`e1c6cf6 docs: record prototype validation and remote sync`

## 2026-09-04：输入监听、SQLite 与真实数据接入

### 本次完成

- [x] 新增 `src-tauri/src/input.rs`：Windows 键盘/鼠标低级 Hook
- [x] 新增统一输入事件模型和物理键去重逻辑
- [x] 默认忽略自动重复和注入型键盘事件
- [x] 新增 `src-tauri/src/storage.rs`：SQLite schema、日期/小时聚合和清空逻辑
- [x] 新增 `get_dashboard`、`get_recording`、`set_recording`、`clear_stats` Command
- [x] 新增 `stats-updated` Event，把聚合快照推送到 Vue
- [x] Vue 首页在 Tauri 环境显示真实 SQLite 数据，浏览器环境保留演示数据
- [x] 空数据库时不再伪显示演示冠军按键
- [x] Hook 启动失败时主界面仍可启动，并输出诊断信息

### 验证结果

- `cargo fmt`：通过
- `cargo test`：通过，4 个测试全部通过
- `npm run build`：通过
- `npm run tauri build -- --bundles msi`：通过
- release executable 启动冒烟检查：通过，进程正常启动并保持运行
- 运行目录已创建 SQLite 文件：`C:\Users\579\AppData\Roaming\com.keyboardmouse.heatmap\keypulse.sqlite`

### 遇到的问题及处理

- Windows 0.61 的 Hook 结构体位于 `WindowsAndMessaging`，已按实际 API 修正
- `SetWindowsHookExW` 需要 `Option<HINSTANCE>`，已修正
- `GetMessageW` 返回 `BOOL`，已修正消息循环
- Tauri setup 中 Arc 状态先被 move 的问题已通过提前 clone 修正
- 完整 NSIS 打包仍受跨磁盘移动错误 `os error 17` 影响；单独 MSI 已成功生成

### 当前未完成

- [ ] 人工确认真实键盘按键能实时增加界面计数
- [ ] 人工确认真实鼠标点击/滚轮能实时增加界面计数
- [ ] 验证暂停/继续和重启持久化
- [ ] 实现本周、本月和自定义日期查询

### 下一步

1. 运行 MSI 或 release exe 做人工输入验收
2. 检查键盘 ID 与模板映射，修正边缘键位
3. 增加日期范围查询和前端筛选
4. 增加系统托盘与开机启动前先补隐私设置
5. 完成输入监听阶段备份并推送

### 阶段备份

- 输入监听 + SQLite 阶段快照：`backups/2026-09-04-input-storage.zip`
- 快照对应提交：`868ddb6 feat: add native input tracking and sqlite aggregation`

## 2026-09-04：时间范围查询与验收清单

### 本次完成

- [x] SQLite 支持 `today`、`week`、`month` 三种日期范围
- [x] Vue 时间切换按钮真正调用对应范围查询
- [x] 实时事件只覆盖“今天”视图，避免覆盖本周/本月查询结果
- [x] 增加清空本地数据按钮和确认提示
- [x] 增加 `docs/人工验收清单.md`，明确真实输入、暂停、持久化和权限限制测试步骤

### 验证结果

- `cargo test`：通过，5 个测试全部通过，包含跨日期范围聚合测试
- `npm run build`：通过
- `npm run tauri build -- --bundles msi`：本次通过，最新范围和清空功能已进入 MSI
- release executable 启动冒烟检查：本次通过

### 下一步

1. 按 `docs/人工验收清单.md` 执行真实输入测试
2. 根据实际测试结果修正 Hook 到 SVG 键位的边缘映射
3. 完成真实历史日期选择器
4. 增加托盘菜单和开机启动前的隐私设置

## 2026-09-04：真实输入统计闭环第一版

### 本次完成

- [x] 新增 Rust 输入事件模型和 Windows Hook 监听模块
- [x] 键盘默认按物理键去重，长按自动重复不会重复计数
- [x] 忽略 Windows 注入型键盘事件
- [x] 统计键盘、鼠标按钮、垂直/水平滚轮
- [x] 新增 SQLite schema、日期/小时聚合和清空逻辑
- [x] 新增 `get_dashboard`、`get_recording`、`set_recording`、`clear_stats` Command
- [x] 新增 `stats-updated` Tauri Event，前端可实时接收聚合快照
- [x] Vue 首页在 Tauri 环境自动切换到本地真实数据，浏览器环境保留演示数据
- [x] 补充输入去重和 SQLite 聚合单元测试

### 验证结果

- `cargo fmt`：通过
- `cargo test`：通过，4 个测试全部通过
- `npm run build`：通过
- `npm run tauri build -- --bundles msi`：通过，MSI 已生成
- release executable 启动冒烟检查：通过，进程正常启动并保持运行

### 编译问题及处理

- Windows 0.61 的 `KBDLLHOOKSTRUCT`/`MSLLHOOKSTRUCT` 实际位于 `WindowsAndMessaging`，已按实际 crate API 修正
- `SetWindowsHookExW` 需要 `Option<HINSTANCE>`，已修正
- `GetMessageW` 返回 `BOOL` 而不是 `Result`，已修正
- SQLite 测试初始排序断言不正确，已改为按实际聚合结果断言

### 当前未完成/待人工验收

- [ ] 实际键盘按键是否能在运行中的界面实时增加
- [ ] 实际鼠标点击、滚轮是否能在运行中的界面实时增加
- [ ] 暂停后不再计数，继续后恢复计数
- [ ] 重启应用后历史统计仍然存在
- [ ] 高权限程序、UAC 和受保护应用的限制说明
- [ ] 本周、本月和自定义日期查询

### 下一步

1. 运行桌面程序进行人工键盘/鼠标验收
2. 根据实际事件结果修正键盘 ID 与 SVG 模板的映射
3. 增加历史日期查询和时间范围接口
4. 完善系统托盘、暂停状态和隐私说明
5. 创建输入监听阶段备份并推送

## 2026-09-04：继续开发后的最终阶段记录

### 本次完成

- [x] 实现 Windows 键盘/鼠标 Hook、自动重复过滤和注入事件过滤
- [x] 实现 SQLite 聚合、today/week/month 查询、清空数据和 Tauri Command
- [x] 接入 `stats-updated` 实时快照事件和 Vue 真实数据渲染
- [x] 增加清空数据控件、真实日期范围按钮和人工验收清单
- [x] 完成最新 MSI 构建和 release 程序启动冒烟检查

### 验证结果

- `cargo test`：5/5 通过
- `npm run build`：通过
- `npm run tauri build -- --bundles msi`：通过
- release executable 启动冒烟检查：通过
- 当前未完成项均需要真实用户操作或后续产品决策，不将其标记为已验证

### 下一步

1. 按 `docs/人工验收清单.md` 手工验证真实键盘、鼠标、暂停和持久化
2. 完成自定义日期选择器
3. 处理 NSIS 打包的跨磁盘临时目录问题
4. 再评估系统托盘、开机启动和应用分类统计

## 2026-09-04：系统托盘控制与阶段收口

### 本次完成

- [x] 启用 Tauri 2 `tray-icon` 能力
- [x] 增加托盘菜单：打开主窗口、暂停/继续记录、清空本地统计、退出应用
- [x] 托盘暂停/继续通过 `recording-changed` 事件同步到 Vue 主界面
- [x] 托盘清空统计后通过 `stats-updated` 事件刷新今日视图
- [x] 更新需求清单、开发流程、技术决策、人工验收和交接说明

### 验证结果

- `cargo fmt`：通过
- `cargo test`：通过，5 个测试全部通过
- `npm run build`：通过
- `npm run tauri build -- --bundles msi`：通过，MSI 已重新生成
- release executable 启动冒烟检查：通过，进程正常启动并保持运行后已结束
- 托盘菜单真实点击、图标显示和跨权限输入场景：尚未人工验收

### 遇到的问题

- 完整 NSIS 打包仍会在 Windows 跨磁盘临时文件移动阶段失败（`os error 17`）；本轮继续使用明确指定的 MSI 构建作为可复现验证路径。

### 下一步

1. 按 `docs/人工验收清单.md` 验证真实键鼠输入、暂停/继续、清空、持久化和托盘菜单
2. 增加自定义历史日期范围选择
3. 评估开机启动和更细的隐私/权限提示
4. 处理 NSIS 构建环境问题并补充发布打包验证

### 备份/提交

- Git commit：`70b83c1 feat: add system tray controls`
- 阶段快照：`backups/2026-09-04-tray.zip`

## 2026-09-04：自定义日期范围查询

### 本次完成

- [x] SQLite 层新增严格校验的自定义日期闭区间查询
- [x] Tauri 新增 `get_dashboard_custom(start, end)` Command，保留原有预设范围接口
- [x] Vue 日历按钮改为自定义日期弹层，支持开始日期、结束日期、最大日期限制和倒序提示
- [x] 自定义范围应用后刷新键盘、鼠标和小时活跃度数据
- [x] 补充 ADR-009、需求清单、开发流程、人工验收和交接记录

### 验证结果

- `cargo fmt --all`：通过
- `cargo test`：通过，6 个测试全部通过，新增自定义日期边界和输入校验测试
- `npm run build`：通过，Vue 类型检查和 Vite 生产构建均通过
- `npm run tauri build -- --bundles msi`：通过，MSI 已重新生成
- release executable 启动冒烟检查：通过，进程启动后保持运行 4 秒并已正常结束
- 真实日期控件点击和范围结果：待人工验收

### 下一步

1. 完成 Tauri MSI 构建及 release 启动冒烟检查
2. 按人工验收清单验证日期控件、真实键鼠输入和托盘菜单
3. 继续处理 NSIS 跨磁盘临时目录问题

### 备份/提交

- Git commit：`ff94274 feat: add custom dashboard date range`
- 阶段快照：`backups/2026-09-04-custom-date.zip`

## 2026-09-04：NSIS 打包环境修复

### 本次完成

- [x] 定位 NSIS `os error 17` 是 Tauri 工具缓存位于 C 盘、项目位于 F 盘导致的跨磁盘移动
- [x] 在 `src-tauri/tauri.conf.json` 启用 `bundle.useLocalToolsDir`
- [x] 增加 `_nsis_temp/` 忽略规则，避免临时 Node 编译缓存进入 Git

### 验证结果

- `npm run tauri build -- --bundles nsis`：通过，生成 NSIS 安装包
- NSIS 输出：`src-tauri/target/release/bundle/nsis/Keyboard Mouse Heatmap_0.1.0_x64-setup.exe`
- MSI 输出：`src-tauri/target/release/bundle/msi/Keyboard Mouse Heatmap_0.1.0_x64_en-US.msi`
- MSI/NSIS 安装卸载流程：尚未人工验证

### 遇到的问题

- 本次尝试生成的 `_nsis_temp/node-compile-cache` 未能被当前终端策略递归删除，已加入 `.gitignore`；它不进入提交和阶段备份。

### 下一步

1. 人工安装并启动 MSI、NSIS，验证卸载结果
2. 按清单验证真实键鼠输入、日期控件和托盘菜单
3. 再考虑开机启动和应用分类统计

### 备份/提交

- Git commit：`56b856d build: stabilize Windows installer packaging`
- 阶段快照：`backups/2026-09-04-windows-packaging.zip`
