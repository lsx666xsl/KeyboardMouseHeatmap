# 开发日志

## 2026-09-04：项目启动与方案确认

### 本次完成

- [x] 在 `F:\TauriProject` 下建立并确认项目目录 `KeyboardMouseHeatmap`
- [x] 将原先位于 `F:\KeyboardMouseHeatmap` 的目录移动到 `F:\TauriProject\KeyboardMouseHeatmap`
- [x] 确认使用 Tauri 2 + Rust + Vue 3 + TypeScript + SQLite
- [x] 确认项目需要键盘统计、鼠标统计、键盘/鼠标模板和模板数据展示
- [x] 确认主题需要活泼、丰富、年轻化
- [x] 建立 docs、notes、backups、src、tests 目录
- [x] 写入开发流程、需求清单、技术决策、README、交接说明和备份说明
- [x] 检查开发环境：Node.js/npm 可用，Rust/Cargo 已安装
- [x] 生成 Tauri 2 + Vue 3 + TypeScript 基础模板
- [x] 将基础模板合并到正式项目目录
- [x] 将项目包名、Rust crate 名称和窗口标题改为项目名称
- [x] 添加 Git SSH 远程仓库 `origin`
- [x] 使用 HTML section/table 风格整理 README

### 验证结果

- 项目路径存在：`F:\TauriProject\KeyboardMouseHeatmap`
- 现有 Tauri 参考项目：`F:\TauriProject\SerialTool`
- Node.js：`v26.1.0`
- npm：`11.13.0`
- Rust/Cargo：已通过系统路径探测确认可用，具体版本将在初始化工程时记录
- Tauri 脚手架：已成功生成并合并
- Git 远程：`git@github.com:lsx666xsl/KeyboardMouseHeatmap.git`
- Tauri CLI：`2.11.4`
- Tauri：`2.11.5`
- Rust/cargo：`1.95.0`
- `npm run build`：通过
- `cargo test`：通过，当前暂无测试用例
- `npm run tauri build`：release executable 编译通过；安装包阶段失败

### 遇到的问题

- 初次使用命令行版本探测时，PowerShell 的 `Get-Command` 没有直接显示 Cargo；通过 `where.exe` 确认 Cargo 位于 Rust 工具链目录，后续命令必要时使用完整路径或刷新 PATH。
- 脚手架目录合并时，包含递归删除的单次命令被安全策略拦截；已改为分步复制，暂时保留 `_tauri_scaffold` 作为本地临时目录，并在 `.gitignore` 中排除。
- 初次前端构建发现 `public` 被错误复制成单个 SVG 文件，已恢复为 `public` 目录并重新构建通过。
- 初次 Rust 测试发现 `main.rs` 仍引用脚手架 crate 名称，已改为 `keyboard_mouse_heatmap_lib` 并测试通过。
- Tauri release 构建在 `src-tauri/target/release/keyboard-mouse-heatmap.exe` 成功生成；MSI/NSIS bundling 收尾时出现 `系统无法将文件移到不同的磁盘驱动器。 (os error 17)`，需要后续统一临时目录盘符或单独指定 bundle 目标。

### 下一步

1. 建立 Rust 输入监听模块接口，但先不保存原始事件
2. 实现键盘和鼠标事件的去重、归一化和统计聚合
3. 用 Tauri Event 把真实统计推送到当前可视化首页
4. 建立 SQLite 数据表和历史查询
5. 记录输入监听限制并创建输入监听阶段备份

### 备份/提交

- Git commit：`71a7111 docs: establish project plan and handoff records`
- 规划文档、基础工程和可视化原型提交待本次验证后生成
- release executable 已验证，完整安装包仍未验证
- 规划阶段快照：`backups/2026-09-04-planning.zip`
- 快照基于上述文档初始提交生成
